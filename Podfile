# Uncomment the next line to define a global platform for your project
platform :ios, '10.3'

target 'Juliet' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Juliet

  target 'JulietTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Quick', '1.2.0'
    pod 'Nimble', '7.0.2'
  end

end
